---
title: "MainDoc"
format: html
editor: visual
self-contained: true
---

```{r}
source('Packages.R') 
source('DataImport.R')
source('NutritionCleaning.R')
source('DataMerging.R')
source('CreatingVariables.R')
source('CreatingGroupedDFs.R')
```

```{r, eval = F}
source('CreatingVariables.R')
```

```{r, eval = F}
source('CreatingGroupedDFs.R')
```

```{r, eval = F}
source('Corrplot.R')
```

```{r}
corrplot((FullCorr[c(1:19),c(2:105)]), method = "color", tl.cex = .6, addrect = 4, rect.lwd = 1.5, is.corr = FALSE, tl.srt = 35)
```

```{r}
NotNeeded <- c(128:139, 19:28, 43:54, 126)
corrplot(FullMergeCorr[-NotNeeded,-NotNeeded],method = "color", tl.cex = .55)
```

```{r}
plot(AllMerge$BMXBMI ~ AllMerge$BMXWAIST)
```

```{r}
plot(AllMerge$DR1TKCAL ~ AllMerge$BMXWAIST)
```

```{r}
plot(AllMerge$DR1TKCAL ~ AllMerge$BMXWAIST)
```

```{r}
plot(AllMerge$DR1TKCAL ~ AllMerge$DR1TNUMF)
```

```{r}
ggplot(data = AllMerge, aes(BMXHT, RFM)) + geom_point(aes(color = as.factor(Un15YN)))
```

```{r}
histogram(AllMerge$BMIObeseNum)
```

```{r}
histogram(AllMerge$RFMObese)
```
